{
  "name": "knot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^24.0.0"
  },
  "scripts": {
    "build": "./scripts/build-knot",
    "pretest": "./scripts/build-knot",
    "test": "node --test",
    "test:focused": "node --test"
  }
}
