# @scope/knot

This package installs the `knot` command.

Another global package can own the `knot` bin.
The last install or update wins.

This binary still needs `curl`, `gzip`, and zlib on the host.
